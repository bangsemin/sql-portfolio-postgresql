# Day 02 — 주문·상품 데이터 분석

> **학습 목표**
> 주문과 상품 데이터를 탐색하고, 집계와 조인을 이용해 판매 성과를 분석한다.
> 각 절의 `SQL 실습 열기` 링크를 클릭하면 해당 실습 파일로 바로 이동할 수 있다.

---

## 학습 흐름

### 00. 분석 환경 점검

`retail` 스키마와 분석 대상 테이블이 정상적으로 준비되었는지 확인한다.

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/00_environment_check.sql)

#### 실행 목적

분석을 시작하기 전에 데이터베이스 구조와 작업 스키마를 확인한다.
테이블이 존재하지 않거나 다른 스키마를 조회하면 이후 쿼리가 실패할 수 있으므로, 가장 먼저 확인하는 절차이다.

#### 핵심 문법
SET search_path TO retail;
현재 SQL 세션에서 기본 작업 스키마를 retail로 지정한다.
information_schema.tables
데이터베이스의 테이블 메타데이터를 조회할 수 있는 시스템 뷰다.
WHERE table_schema = 'retail'
retail 스키마에 포함된 테이블만 조회한다.
기대 결과

아래 테이블이 조회되면 정상이다.

customers
orders
products
실행 결과
결과 확인 후 작성
해석
결과 확인 후 작성
추가 학습 메모

information_schema는 테이블 구조와 컬럼 정보 등을 확인할 때 사용하는 표준 메타데이터 영역이다.

#### 실행 결과

* `retail` 스키마에서 `customers`, `orders`, `products` 테이블이 정상적으로 조회되었다.
* Day 02 분석에 필요한 주문·상품 데이터가 준비된 상태임을 확인했다.

#### 해석

* 현재 세션에서 `retail` 스키마를 기준으로 쿼리를 실행할 수 있다.
* 이후 주문 분석과 상품 분석은 `orders`, `products` 테이블을 중심으로 진행한다.

#### 추가 학습 메모

* 분석 시작 전 스키마와 테이블 존재 여부를 확인하면 `relation does not exist` 같은 오류를 예방할 수 있다.
* `information_schema.tables`는 데이터베이스의 테이블 목록을 확인할 때 유용하다.


---

### 01. 주문 데이터 구조 확인

주문 분석 전에 `orders` 테이블의 컬럼 구성과 실제 데이터 형태를 확인한다.

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/01_orders_preview.sql)

#### 실행 목적

집계나 조인을 시작하기 전에 주문 데이터가 어떤 기준으로 저장되어 있는지 확인한다.
특히 주문 번호, 고객 번호, 상품 번호, 주문 수량, 주문 일자처럼 이후 분석 기준이 되는 컬럼을 파악한다.

#### 핵심 문법

* `SELECT *`

  * 테이블의 모든 컬럼을 조회한다.
  * 처음 보는 테이블의 구조를 탐색할 때 사용한다.

* `LIMIT 10`

  * 조회 결과를 앞의 10개 행으로 제한한다.
  * 전체 데이터를 조회하지 않고 표본 데이터로 구조를 빠르게 확인할 수 있다.

#### 확인할 항목

* 주문을 식별하는 컬럼은 무엇인가?
* 고객과 상품을 연결하는 컬럼은 무엇인가?
* 주문 수량과 주문 일자는 어떤 형식으로 저장되어 있는가?
* NULL 값 또는 비정상적인 값이 보이는가?

#### 실행 결과

`orders` 테이블의 주요 컬럼과 자료형을 확인했다.

| 컬럼명        | 자료형  | 역할                                 |
| ------------- | ------- | ------------------------------------ |
| `order_id`    | integer | 주문을 고유하게 식별하는 기본 키(PK) |
| `customer_id` | integer | 주문 고객을 식별하는 값              |
| `product_id`  | integer | 주문 상품을 식별하는 값              |
| `order_date`  | date    | 주문 일자                            |
| `quantity`    | integer | 주문 수량                            |

표본 조회 결과에서 주문 4건이 확인되었으며, 주문 일자와 수량이 정상적인 형식으로 저장되어 있었다.

#### 해석

* `order_id`는 각 주문을 구분하는 기본 키로 사용된다.
* `customer_id`는 이후 `customers` 테이블과 연결할 수 있는 기준값이다.
* `product_id`는 `products` 테이블과 연결할 수 있는 기준값이다.
* `orders` 테이블에는 상품 가격이 없으므로, 매출 분석을 하려면 `products` 테이블의 가격 정보와 조인해야 한다.
* 주문 수량은 `quantity` 컬럼으로 관리되며, 판매량과 매출 계산의 기준이 된다.

#### 추가 학습 메모

* `orders`는 거래 사실을 저장하는 테이블이며, 고객·상품의 상세 정보는 별도 테이블에서 관리하는 구조다.
* 이런 구조는 중복 데이터를 줄이고 데이터 변경을 쉽게 하기 위한 관계형 데이터베이스 설계 방식이다.
* `customer_id`, `product_id`는 다른 테이블의 기본 키와 연결되는 외래 키(FK) 역할을 한다.



---

### 02. 주문 데이터 기본 집계

주문 건수와 주문 수량의 기본 현황을 확인한다.

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/02_orders_summary.sql)

#### 실행 목적

주문 데이터의 전체 규모와 주문 수량 분포를 숫자로 파악한다.
분석을 시작할 때 가장 먼저 확인하는 기본 지표다.

#### 핵심 문법

* `COUNT(*)`

  * 전체 행 수, 즉 전체 주문 건수를 계산한다.

* `SUM(quantity)`

  * 모든 주문 수량을 더해 총 판매 수량을 계산한다.

* `AVG(quantity)`

  * 주문 1건당 평균 판매 수량을 계산한다.

* `MIN(quantity)`, `MAX(quantity)`

  * 가장 적은 주문 수량과 가장 많은 주문 수량을 확인한다.

#### 실행 결과

* 실행 후 기록

#### 해석

* 실행 후 기록

#### 추가 학습 메모

* 집계 함수는 여러 행의 데이터를 하나의 결과값으로 요약한다.
* `COUNT(*)`는 NULL 여부와 무관하게 모든 행을 계산한다.

---

### 03. 주문 데이터 품질 점검

NULL 값, 중복 가능성, 비정상 주문 수량 등 분석 전 확인이 필요한 항목을 점검한다.

**핵심 문법** `NULL`, `COUNT`, `GROUP BY`, `HAVING`

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/03_orders_quality_check.sql)

---

### 04. 상품 데이터 구조 확인

상품명, 카테고리, 가격 등 상품 테이블의 기준 정보를 확인한다.

**핵심 문법** `SELECT`, `LIMIT`, `ORDER BY`

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/04_products_preview.sql)

---

### 05. 주문별 매출 계산

주문 수량과 상품 가격을 활용해 주문 단위 매출을 계산한다.

**핵심 문법** 산술 연산, `AS`

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/05_order_revenue.sql)

---

### 06. 상품별 판매 성과 분석

상품별 주문 건수, 판매 수량, 매출을 집계하고 순위를 확인한다.

**핵심 문법** `GROUP BY`, `SUM`, `ORDER BY`

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/06_product_sales_analysis.sql)

---

### 07. 카테고리별 매출 분석

상품 카테고리별 판매 수량과 매출을 비교하여 주요 매출원을 파악한다.

**핵심 문법** `INNER JOIN`, `GROUP BY`, `SUM`

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/07_category_sales_analysis.sql)

---

### 08. 주문·상품 조인 분석

주문과 상품 테이블을 연결하고 PK·FK 관계와 조인의 동작 원리를 이해한다.

**핵심 문법** `INNER JOIN`, PK, FK

[SQL 실습 열기 →](../02_projects/day02_order_product_analysis/08_orders_products_join.sql)

---

## Day 02 핵심 질문

* 주문 데이터는 어떤 구조로 저장되어 있는가?
* 주문 수량과 가격으로 매출을 어떻게 계산하는가?
* 어떤 상품과 카테고리가 판매 성과에 가장 크게 기여하는가?
* `orders`와 `products`는 어떤 키를 기준으로 연결되는가?

---

## 학습 기준

* **SQLD**: 집계 함수, `GROUP BY`, `HAVING`, 조인의 차이와 실행 원리를 설명할 수 있어야 한다.
* **실무**: 분석 전에 데이터 구조와 품질을 확인하고, 지표를 정의한 뒤 결과를 해석한다.
* **포트폴리오**: 쿼리뿐 아니라 분석 목적, 결과, 인사이트를 함께 기록한다.
